## What and why

<!-- What behaviour changes, and what problem it solves. Link an issue if there is one. -->

## Testing

<!-- Which tiers did you run? e.g. `./tests/run_tests.sh --only unit:kotlin` -->

- [ ] `./gradlew ktlintCheck` passes
- [ ] `./tests/run_tests.sh --only unit:kotlin` passes
- [ ] New behaviour has a test, or the fix has a regression test

## Notes for the reviewer

<!-- Trade-offs, follow-ups, anything deliberately left out. -->
