Marathon Shepherd {{ .Chart.AppVersion }} is starting as {{ include "marathon-shepherd.fullname" . }}.

Reach the API:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "marathon-shepherd.fullname" . }} {{ .Values.service.port }}
  export MSH_URL=http://localhost:{{ .Values.service.port }}

{{ if or .Values.adminPassword.value .Values.adminPassword.existingSecret -}}
Sign in as `admin` with the password you provided in adminPassword.
{{- else -}}
The manager created the user `admin` with a one-time password and printed it once:

  kubectl --namespace {{ .Release.Namespace }} logs deploy/{{ include "marathon-shepherd.fullname" . }} | grep -A3 "first admin account"

A copy is in /var/lib/msh/initial-admin-password inside the pod. Sign in, choose your own
password, then delete that file.
{{- end }}

  mshctl login --username admin
  mshctl health
  mshctl devices
{{ if not .Values.persistence.enabled }}
WARNING: persistence is disabled. Sessions, users and the generated admin password are lost when the pod
restarts. Set persistence.enabled=true, or point database.url at Postgres.
{{- end }}
